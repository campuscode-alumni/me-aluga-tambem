<h1>Novo tipo de imóvel</h1>

<ul>
<% @property_type.errors.full_messages.each do |message| %>
  <li><%= message %></li>
<% end %>
</ul>

<%= form_for @property_type do |f| %>
  <div class="form-group">
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <%= f.submit 'Cadastrar', class: 'btn btn-primary' %>
<% end %>
